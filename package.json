{
  "name": "@notnuzzel/prisma",
  "version": "1.0.13",
  "main": "index.js",
  "files": [
    "index.js",
    "schema.prisma"
  ],
  "devDependencies": {
    "@vercel/ncc": "^0.28.6",
    "dotenv": "^10.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@prisma/client": "^2.24.1",
    "prisma": "^2.24.1"
  },
  "scripts": {
    "test": "dotenv ncc run test/",
    "generate": "prisma generate",
    "postinstall": "prisma generate"
  }
}
